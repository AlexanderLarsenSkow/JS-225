/*
Modules: similar to gems

importing code

readline-sync:

	const readline = require('readline-sync');
	let choice = readline.question('Do you want to run this program? (Y/N)');

	install with NPM, use require to use it in program
*/